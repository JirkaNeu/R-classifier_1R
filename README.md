# R-classifier_1R

a "1R" or "OneR" classifier written in R - a simple classifier for binary characteristics

This script is using the "Auto" dataset of the "ISLR" package. US amarican and japanese cars are being classified as those models with the higher quantity.
Downsampling is used to balance the data (upsampling comes next).
The script tries to abstain from R libraries/packages.
