# R-classifier_1R

a "1R" or "OneR" classifier written in R - a simple classifier for binary characteristics

This script is using the "Auto" dataset of the "ISLR" package. US amarican and japanese cars are being classified as such by quantity.
Downsampling is used to balance the data (upsampling in the making).
The script tries to abstain from R libraries/packages.
